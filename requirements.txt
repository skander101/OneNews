# Core
praw>=7.7.0
trafilatura>=1.6.0
requests>=2.28.0
python-dotenv>=1.0.0

# NLP / GenAI (optional - program falls back to simple algorithms if missing)
transformers>=4.30.0
sentence-transformers>=2.2.0
torch>=2.0.0
numpy>=1.24.0

# Install with: pip install -r requirements.txt
# For CPU-only: pip install torch --index-url https://download.pytorch.org/whl/cpu
